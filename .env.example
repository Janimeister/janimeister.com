# Optional: live YouTube feed endpoint exposed by the Cloudflare Worker.
# When set, the site refreshes the chronicle list on load. Without it,
# only the build-time `videos.json` is used (still fully functional).
VITE_VIDEO_API=
