name = "janimeister-feed"
main = "src/index.ts"
compatibility_date = "2025-09-01"

# Configure your channel + allowed origins.
[vars]
CHANNEL_ID = "UCvCde3OAobvTuLdeiCpFDGw"
ALLOWED_ORIGIN = "https://janimeister.com"

# YT_API_KEY must be set as a secret, NOT here (to avoid committing it).
# Run once: wrangler secret put YT_API_KEY

[observability]
enabled = true
